import React from 'react';
import { expect } from 'chai';

import TestCmpt from './TestComponentWithFetch';

describe('Tooltip', () => {
    it('should run', function() {
        expect(true, 'dummy test').to.be.true;
    });
});
